# This is an intentionally wrong config file. The mongodb section does not have
# an index_keys field

[mongodb]
address = ctecho01
authSource = ds_testing
collection = metadata_ingester_tests_config
database = ds_testing
max_json_doc_size = 16777216
port = 27017
username = ds_testing

[secrets]
filename = secrets.cfg
